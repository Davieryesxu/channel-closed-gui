[project]
name = "channel-closed-gui"
authors = ["Alice Wordsmith"]
description = "A decentralized platform for creative writing challenges"
telemetry = false
requirements = []
[contracts.channel-closed]
path = "contracts/channel-closed.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
